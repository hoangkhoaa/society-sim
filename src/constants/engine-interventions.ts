/**
 * God-agent / manual intervention limits (`engine/interventions.ts`).
 */

/** Circular buffer cap for `npc.memory` entries. */
export const MAX_NPC_MEMORIES = 10

/** Maximum number of entries retained in the breakthrough log. */
export const MAX_BREAKTHROUGH_ENTRIES = 50
